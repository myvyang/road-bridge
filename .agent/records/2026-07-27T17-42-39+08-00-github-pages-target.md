# Session Record: Switch deployment target to GitHub Pages

- Time: 2026-07-27T17:42:39+08:00 Asia/Shanghai
- Window: 2026-07-27T17:30:07+08:00 Asia/Shanghai to 2026-07-27T17:42:39+08:00 Asia/Shanghai
- Previous Record: `.agent/records/2026-07-27T17-30-07+08-00-amap-map-provider.md`
- Commit: self, recorded in the same commit as this file
- Branch: main
- Task: Correct the deployment target from OpenAI Sites to GitHub Pages under `myvyang/road-bridge`.
- Source Sessions:
  - Harness: Codex
  - Evidence: current conversation; user clarified that the site should publish under GitHub Pages, not Sites.
  - Checked: current repository scripts, build config, project memory, GitHub Pages official docs, Next static export docs, GitHub Pages action docs, local build/test output.
  - Used: GitHub Pages custom workflow guidance; Next static export behavior; current official actions versions for checkout/setup-node/upload-pages-artifact/deploy-pages.
  - Unavailable: remote GitHub Actions run result is not available until the commit is pushed and the repository Pages Source is set to GitHub Actions.

## Outcome

- Added `.github/workflows/pages.yml` to build a static export and deploy `out/` to GitHub Pages on pushes to `main`.
- Converted the project from Vinext / Cloudflare Worker default scripts to plain Next scripts.
- Removed OpenAI Sites, Vinext, Cloudflare Worker, D1, Drizzle, and Vite scaffold files and dependencies that are not needed for GitHub Pages.
- Kept `build:pages` as the explicit static-export build path with `GITHUB_PAGES_BASE_PATH=/road-bridge`.
- Added `generateStaticParams()` for stock symbol pages so `/stocks/[symbol]` is exported as static HTML.
- Prefixed exported asset and favicon paths for the GitHub Pages subpath `/road-bridge`.
- Updated README and project memory to make GitHub Pages the deployment target.
- Updated tests to validate exported static HTML instead of the old Worker response.

Verification:

- `PATH="$HOME/.volta/tools/image/node/22.13.0/bin:$PATH" npm run lint` passed.
- `PATH="$HOME/.volta/tools/image/node/22.13.0/bin:$PATH" npm test` passed.
- `PATH="$HOME/.volta/tools/image/node/22.13.0/bin:$PATH" NEXT_TELEMETRY_DISABLED=1 npm run build` passed.
- Local Next dev server was restarted and `http://localhost:3000/` returned 200.
- Exported HTML paths were inspected and use `/road-bridge` for static assets after the favicon fix.

## Engineering Context

The earlier Sites 403 is no longer a project blocker because Sites is not the intended host. The durable deployment rule is now: GitHub Pages is a static host, so future app features must remain static-export-compatible unless the deployment target changes again.

The expected production URL for the current repository is `https://myvyang.github.io/road-bridge/`. The repository must have Pages Source set to `GitHub Actions`; the AMap Web key should be configured as GitHub Actions secrets if real basemap rendering is needed in production.

## Open Questions And Risks

- The first GitHub Actions deployment still needs to run after this commit is pushed.
- The user may need to enable GitHub Pages or set Source to `GitHub Actions` in repository settings.
- AMap production rendering still depends on `NEXT_PUBLIC_AMAP_KEY` and optional `NEXT_PUBLIC_AMAP_SECURITY_CODE` being configured in GitHub Actions secrets.
